---

    mail_smarthost: "localhost"

    admin_mail: ""

    install_prefix: "/opt/SI5000"

    docker_network_name: ansible-net

    ct_image_name: "anmcarrow/ansible:latest"
    ct_volumes_prefix: "{{ install_prefix }}/ansible"
    ct_test_pattern: "ansible/ [0-9].[0-9].[0-9]"
